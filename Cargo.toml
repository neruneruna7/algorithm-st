[package]
name = "algorithm-st"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
    ".",
    "ram-lsp",
    "ram-syntax"
]


[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
ram-syntax = { path = "ram-syntax" }
